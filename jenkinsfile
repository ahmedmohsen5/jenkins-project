pipeline {
    agent {
        label 'agent1'
    }
    environment {
        DOCKER_HOST = 'tcp://jenkins-docker:2375'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'docker build -t my-app:latest devops-helper'
            }
        }
    }
}

